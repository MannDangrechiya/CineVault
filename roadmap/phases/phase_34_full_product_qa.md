# Phase 34 — Full Product QA
**Goal:** Full end-to-end scenario testing.

## Scenario 1 — Core loop
new user → login → search movie → open title → add watchlist → watch → rate
→ review → statistics update → recommendation appears

## Scenario 2 — TV
TV series → season → episode → progress → completion → history

## Scenario 3 — Offline
offline → watch episode → create rating → reconnect → sync → verify server

## Scenario 4 — Metadata update safety
metadata update → canonical change → personal history preserved

## Scenario 5 — Conflict resolution
provider conflict → conflict queue → curator resolution → canonical update
→ provenance retained

## Scenario 6 — Recommendations
AI recommendation → explanation → user feedback → recommendation quality
