# Phase 14 — AI Assistant Foundation
**Goal:** Provider-agnostic AI architecture. AI must NOT become canonical authority.

## Architecture
```
User → AI Assistant → CineVault context → AI provider → structured proposal
     → validation → evidence → human approval where required → canonical system
```

## AI-generated metadata must carry
source, model, prompt version, timestamp, confidence, review status.
