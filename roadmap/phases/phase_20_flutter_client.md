# Phase 20 — Flutter Client
**Goal:** Build the client after backend contracts are stable.

## Implement
authentication, home, search, catalog, title detail, watchlist, library,
history, ratings, reviews, collections, profile, statistics, recommendations,
AI assistant, offline mode, sync status.

## Constraint
Use the backend API contract. Do not duplicate canonical business logic inside Flutter.
