# Pinned to exact versions (not >=) as of 2026-09-01 — R1 fix, same
# rationale as the root requirements.txt: matches this project's dev
# environment as actually installed and verified working.
celery==5.6.3
redis==8.1.0
pydantic==2.13.4
sqlalchemy==2.0.51
asyncpg==0.31.0
pgvector==0.5.0
httpx==0.28.1
pyyaml==6.0.3
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
Unidecode==1.4.0
